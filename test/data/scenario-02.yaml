input: |
    Hello world

"rules": [
  {
    "#": "Regex match for a=1234",
    "match": "(?m)^a=(?P<a>\\d+)",
    "template": "The value of a is {{ a }}"
  },
  {
    "action": "drop",
    "match": "^a=(?P<a>\\d+)"
  },
  {
    "action": "send",
    "match": "a regex with capture groups"
  },
  {
    "action": "send",
    "if": "{{ jinja predicate }}",
    "template": "{{ jinja thingy }}"
  },
  {
    "template": "The value of a is `{{ a }}`\n```\nWHEE\n```\n\n# That's all folks\n\n...\n"
  }
]

output: |
  I'm a little teapot
